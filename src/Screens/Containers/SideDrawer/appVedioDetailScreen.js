import {connect} from "react-redux";
import {AppVedioDetailScreen} from '../../Components/SideDrawer/appVedioDetailScreen';

const mapStateToProps = (state) => {
    return {};
};

const mapDispatchToProps = (dispatch) => {
    return {}
};

export default connect(mapStateToProps, mapDispatchToProps)(AppVedioDetailScreen);
