import {connect} from "react-redux";
import {Faq} from '../../Components/SideDrawer/faq';

const mapStateToProps = (state) => {
    return {};
};

const mapDispatchToProps = (dispatch) => {
    return {}
};

export default connect(mapStateToProps, mapDispatchToProps)(Faq);
